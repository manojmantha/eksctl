
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
    name: expense
    region: us-east-1
managedNodeGroups:
  - name: roboshop-dev
    instanceTypes: ["t3.small"]
    desiredCapacity: 3 #  by default this value is 3
    spot: true
